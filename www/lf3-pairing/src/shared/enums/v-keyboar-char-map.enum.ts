export enum KeyboardCharMap {
  Delete = "{bksp}",
  Shift = "{shift}",
  Alt = "{alt}",
  Space = "{space}",
  Enter = "{enter}",
  Default = "{default}",
}

export enum LayoutName {
  Default = "default",
  Shift = "shift",
  Alt = "alt",
}
