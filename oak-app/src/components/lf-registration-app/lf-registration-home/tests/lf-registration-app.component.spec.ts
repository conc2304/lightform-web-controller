import { LfRegistrationApp } from '../lf-registration-app.component';
// import { newSpecPage } from '@stencil/core/testing';

describe('lf-registration-app', () => {
  it('renders', async () => {
    expect(new LfRegistrationApp()).toBeTruthy;
  });
});
