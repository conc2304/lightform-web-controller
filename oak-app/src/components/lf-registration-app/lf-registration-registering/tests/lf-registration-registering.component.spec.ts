import { LfRegistrationRegistering } from "../lf-registration-registering.component"
// import { newSpecPage } from '@stencil/core/testing';


describe("lf-registration-registering", () => {
  it("should build", () => {
    expect(new LfRegistrationRegistering()).toBeTruthy;
  });
});
