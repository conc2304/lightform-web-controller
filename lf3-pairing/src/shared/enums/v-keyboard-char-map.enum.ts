export enum KeyboardCharMap {
  Delete = "{bksp}",
  AlphaShift = "{shift}",
  Numeric = "{numeric}",
  NumericShift = "{numericShift}",
  Space = "{space}",
  Enter = "{enter}",
  Alpha = "{alpha}",
}

export enum LayoutName {
  Alpha = "alpha",
  AlphaShift = "shift",
  Numeric = "numeric",
  NumericShift = "numericShift",
}

